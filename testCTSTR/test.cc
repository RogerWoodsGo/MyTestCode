#include <iostream>
#include <string>

using namespace std;


int main(){
    const char *a = NULL; 
    string cstr = "";
    string cstr1;
    bool b = (cstr1 == cstr);
    cout << b <<"sp" << cstr << cstr.size() << endl;
    return 0;
}

